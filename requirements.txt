# ScoutRAG — HuggingFace Spaces dependencies (Python 3.10)
pandas>=2.0
numpy>=1.24

# Semantic retrieval
sentence-transformers>=2.2
scikit-learn>=1.3
faiss-cpu>=1.7

# LLM API — set ANTHROPIC_API_KEY as a Space Secret to enable LLM features.
# Without it the system falls back to rule-based query parsing and
# deterministic report templates (fully functional, just less fluent).
anthropic>=0.28
audioop-lts; python_version >= "3.13"
gradio>=5.0
