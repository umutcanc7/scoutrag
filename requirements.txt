# ScoutRAG — local dependencies (Python 3.10+)
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.12

# Semantic retrieval (CPU is fine on Apple Silicon; MPS used automatically if available)
sentence-transformers>=2.2
scikit-learn>=1.3

# Optional exact-NN index. faiss-cpu works on macOS; sklearn cosine is the fallback.
faiss-cpu>=1.7 ; platform_system != "Darwin" or platform_machine != "arm64"

# Local API backend for the web UI
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# The local LLM (Ollama) is a separate native app — install via `brew install ollama`,
# then: `ollama pull llama3.1` (or set DEFAULT_MODEL in scoutrag/llm.py to your model).
# No Python package required; we call its HTTP API.
